# Simple Status API

A basic Node.js API built with Express for training and practice.

## Endpoints

GET /
Returns a welcome message.

GET /status
Returns API status.

GET /about
Returns project information.

## How to run locally

Install dependencies:

npm install

Start the server:

node app.js

Server runs on:

http://localhost:3000